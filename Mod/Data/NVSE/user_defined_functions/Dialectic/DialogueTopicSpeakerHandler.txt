ref rTopicOrInfo
ref rSpeaker

begin Function {rTopicOrInfo}
    let rSpeaker := this
    if eval !(rSpeaker) || !(IsFormValid rSpeaker)
        return
    endif

    if eval rTopicOrInfo && IsFormValid rTopicOrInfo
        DialecticCaptureDialoguePrompt rSpeaker rTopicOrInfo
    endif
end
