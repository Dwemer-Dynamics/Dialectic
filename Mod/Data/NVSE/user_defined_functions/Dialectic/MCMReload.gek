float fValue
int iMenu
int iSubmenu
int iOption
array_var aJSON

Begin Function {fValue, iMenu, iSubmenu, iOption, aJSON}
    ; MCM Extender caches INI writes while the menu is open. The
    ; MCMExtUpdate handler applies settings after that cache is saved.
    return
End
